import FileAnalyConfig from "./file-analy-config-field";

const Loading = {
    load: function (Gfui) {
        Gfui.grid('file-analy-config-field', FileAnalyConfig);
    }
};

export default Loading;
