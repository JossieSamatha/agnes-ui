import CustomRuleList from './basic-info/custom-rule-list'

const Loading = {
    load: function (Gfui) {
        Gfui.grid('agnes-custom-rule-list', CustomRuleList);
    }
};

export default Loading;
