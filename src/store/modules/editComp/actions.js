export default {
    // initStateData({commit}, params){
    //     return new Promise((resolve) => {
    //         GFIndexApi.login(params.data, params.publicKey).then((resp) => {
    //             if(!resp || !resp.ok) {
    //                 resolve(resp);
    //                 return;
    //             }
    //             commit('resetUser');
    //             //储存用户信息
    //             commit('setUser', resp.data);
    //             resolve(resp);
    //         });
    //     });
    // }
};
